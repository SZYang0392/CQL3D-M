2026.6.23 Update
1. Compiling update
    (1). Add files for compiling on win10 :
            pgplot_stubs.f90 : for lack of pgplot packages on win10
            ./mpi/doparallel3.py : python 3 version of ./mpi/doparallel.py
            makefile_win : windows version of makefile
            make_win.bat : set necessary environments and call makefile_win
        make commands : make_win.bat (-j6)
        clean commands : make_win.bat clean
    (2). Modified make commands : accelerate calculation and remove debugging settings
        Modified/Added files : makefile_win, makefile_shenma, makefile_newshenma
    (3). File 'makefile' is removed. Use 'make -f <filename>' to compile on a 
        specific platform.
    (4). All of the 3 make commands support '-j' : 
        makefile_win, makefile_shenma, makefile_newshenma
2. Bugs fixed for NBI simulation in CFETR
    (1). ainvnorm.f : fixed the 'ion mess = 0' bug in no-general-electrons runs.
    (2). impavnc0.f : size of array 'iwk_ilu' is set larger for CFETR NBI cases.
    (3). tdtrdfus.f : fixed the bug that indexes of array 'ynewx' might run out
        of bounds when transp = 'enabled'.
    (4). Some unused debugging files are stored in ./CFETR_LHW_NB_debug_backup.
        see ./CFETR_LHW_NB_debug_backup/README.md
3. Add reference cases for CFETR NBI simulations which have run successfully on 
    login110 platform of the shenma cluster. See 
    ./CFETR_LHW_NB/README.md
4. AGENTS.md : file generated by cursor agents and perhaps useful for AI coding.